on:
  push:
    branches:
    - main
    - master
    - release/*
    - hotfix/*
    - hotfixes/*

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps: 
    - uses: CultureHQ/actions-yarn@v1.0.1
    - uses: ./
    - run: yarn install
    - run: yarn test
